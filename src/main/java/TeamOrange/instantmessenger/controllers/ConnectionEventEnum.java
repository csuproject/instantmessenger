package TeamOrange.instantmessenger.controllers;

/**
 * Represents all possible connection event types
 *
 */
public enum ConnectionEventEnum {
	DISCONNECTED, RECONNECTION_SUCCEEDED, RECONNECTION_FAILED, RECONNECTION_PENDING;
}
